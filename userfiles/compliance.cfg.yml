---
DIR:
    - NAME: /usr/bin
      CMD: 
          - "ls -ld"
    - NAME: /usr/lib

FILE:
    - NAME: /etc/ssh/sshd_config
      CHK: on
      CON: on
      CMD:
          - "ls -la"
    - NAME: /etc/passwd
      CHK: on   
