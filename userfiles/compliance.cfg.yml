---
outgoing_interface: "GigabitEthernet0/0/0/0"

DIR:
    - NAME: /usr/bin
      CMD: 
          - "ls -ld"
    - NAME: /root
      CMD:
          - "ls -lrt"
          - "touch test.txt"
    - NAME: /misc/scratch
    - NAME: /misc/app_host
    - NAME: /etc
    - NAME: /
FILE:
    - NAME: /etc/ssh/sshd_config
      CHK: on
      CON: on
      CMD:
          - "ls -la"

    - NAME: /etc/passwd
      CHK: on 
      CON: on

    - NAME: /usr/bin/python
      CHK: on
      CMD:
          - "ls -la"

    - NAME: /etc/default/dhcp-server
      CHK: on
      CON: on
      CMD:
          - "ls -lhrt"

